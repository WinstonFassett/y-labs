---
import Layout from "@/layouts/LabLayout.astro";
import App from "@/app/milkdown/App";
const frontmatter = {
  title: "Milkdown",
  favicon: "/y-labs/logos/milkdown.svg",
};
---

<Layout frontmatter={frontmatter} class="min-h-dvh">
  <App client:only="react" frontmatter={frontmatter} />
</Layout>
