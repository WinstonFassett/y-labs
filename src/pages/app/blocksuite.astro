---
import Layout from "@/layouts/LabLayout.astro";
import App from "@/app/blocksuite/App";
const frontmatter = {
  title: "BlockSuite",
  favicon: "/y-labs/logos/blocksuite.svg",
};
---

<Layout frontmatter={frontmatter} class="h-dvh">
  <App client:only="react" frontmatter={frontmatter} />
</Layout>
